# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*

# typescript
*.tsbuildinfo

# Shortlist video assets (~700MB) live on the main branch; they exceed this
# remote's push size limit, so they are not tracked on this branch. See the
# "Set the 30 shortlisted videos as the voting reels" commit.
/public/shortlist-videos/
