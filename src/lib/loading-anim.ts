// The interstitial clip that plays between reels. Short and small so it
// loads instantly and serves as a buffering window for the next real reel.
export const LOADING_ANIM = {
  reel_id: "__loading_anim__",
  file_path: "/loading%20anim.mp4",
  runtime: 7,
} as const;
